async function search() {
  const q = document.getElementById("query").value;
  const result = document.getElementById("result");

  result.innerText = "Thinking...";

  result.innerText = "AI answers coming next (API setup step)";
}
